{
  "name": "nexusguard-ai",
  "version": "1.0.0",
  "description": "Autonomous security scanner — GitHub Webhook receiver.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js",
    "webhook:setup": "node scripts/setup-webhook.js",
    "install:wsl": "node $NVM_BIN/../lib/node_modules/npm/bin/npm-cli.js install"
  },
  "engines": {
    "node": ">=20.6.0"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "dockerode": "^5.0.0",
    "express": "^4.22.2",
    "ws": "^8.18.2"
  }
}
